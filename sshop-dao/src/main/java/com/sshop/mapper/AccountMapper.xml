<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sshop.dao.AccountDao">
	<insert id="insert">
	  	insert into Account (
	  		pwd, usable, locked, code18, name, phone
	  	) values (
	  		#{pwd}, 1, 0, #{code18}, #{name}, #{phone}
	  	)
	  </insert>
</mapper>