<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sshop.dao.ImagesDao" >
  
  <insert id="insert" useGeneratedKeys="true" keyColumn="id">
  	insert into IMAGES (
				userId,
				goodsCode,
				detailCode,
				partId,
				type,
				description,
				imagesName,
				status,
				path,
				sort
			) values (
				#{userId},
				#{goodsCode},
				#{detailCode},
				#{partId},
				#{type},
				#{description},
				#{imagesName},
				#{status},
				#{path},
				#{sort}
			)
  </insert>
  
</mapper>